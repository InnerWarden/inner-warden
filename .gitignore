
.claude/
.DS_Store
.env
.env.local
.specify/
*.key
*.log
*.p12
*.pem
**/node_modules
**/target
/target
# belong to the operator, not to a public repository.
# Environment and secrets
# graphify code graph: 81M of local build artefacts, never committed
# Internal product notes. Working documents that describe commercial posture,
# Local-only specs, never committed
# Node
# OS and logs
# Rust
# the paid tier's install behaviour, and where it currently falls short. They
graphify-out/
PRODUTO.md
