name: Publish to npm (OIDC)

# Least privilege (audit CI-11). `id-token` is required for OIDC provenance and
# is granted at the JOB that publishes, not repo-wide.
permissions:
  contents: read

# Publishes the Community CLI to npm using GitHub OIDC trusted publishing.
# No long-lived npm token and no 2FA bypass: authentication is short-lived and
# scoped to this workflow, and each package gets a signed provenance
# attestation linking it back to this repo and build.
#
# Prerequisite (one-time, on npmjs.com): configure a Trusted Publisher for the
# `innerwarden` package and each `@innerwarden/cli-<os>-<arch>` package (or an
# org-level trusted publisher for the @innerwarden scope), pointing at:
#   repository:        InnerWarden/inner-warden
#   workflow filename: npm-publish.yml
#
# Release flow: bump the version in npm/package.json and push a `guard-v*` tag.
# The release workflow publishes the binaries and this one chains off it, so
# npm never lags the bytes it wraps. The manual trigger and the `npm-v*` tag
# remain for republishing a channel on its own.

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Pack and validate without publishing"
        type: boolean
        default: false
  push:
    tags:
      - "npm-v*"

  # `npm install -g innerwarden` is the primary install path, and it had no
  # trigger tied to the release: the binaries went out under a `guard-v*` tag
  # while npm sat on whatever was last pushed by hand. It served 1.3.3 for the
  # length of a release whose binaries were 1.3.4, so the documented install
  # command handed users an older CLI than the one just shipped. Chain off the
  # release so a cut moves this channel too.
  #
  # Safe to re-run: npm/scripts/publish.mjs treats a version already on the
  # registry as success, so a re-cut of the same version is a no-op, not a
  # red workflow.
  workflow_run:
    workflows: ["Release InnerWarden Community"]
    types: [completed]

concurrency:
  group: npm-publish
  cancel-in-progress: false

jobs:
  # Audit CI-04: publishing required no green CI and no human in the loop, so a
  # tag on a commit whose tests were failing would publish anyway, and npm
  # provenance would attest it. This gate answers the checks API for the exact
  # commit being published and refuses when CI is not green on it.
  gate:
    name: CI must be green on this commit
    runs-on: ubuntu-latest
    # A release that failed must not publish to npm.
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    permissions:
      # `actions: read` is what the Actions API this job queries actually needs.
      # It ran with `checks: read` alone, which is a different API; the call
      # happened to succeed, so the gap would only surface as a publish failing
      # for an unrelated-looking reason on the day GitHub tightened the default.
      actions: read
      checks: read
      contents: read
    steps:
      # Deliberately the DEFAULT checkout, never `workflow_run.head_sha`.
      # This job publishes to npm with OIDC; running a commit named by the event
      # payload would hand that identity to whatever the event pointed at.
      # CodeQL calls it `actions/untrusted-checkout/critical` and it is right.
      # The version consistency this would have bought is already enforced
      # further down by npm/scripts/build.mjs, which runs `--version` on the
      # actual binary and refuses to publish bytes that disagree with the
      # version number being published.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Require a green CI run for ${{ github.sha }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sha="${{ github.sha }}"
          runs=$(gh api "repos/${{ github.repository }}/actions/workflows/ci.yml/runs?head_sha=$sha&per_page=20" \
            --jq '[.workflow_runs[] | select(.status=="completed")] | map(.conclusion)')
          echo "CI conclusions for $sha: $runs"
          # Check every conclusion, unconditionally.
          #
          # This was wrapped in `if echo "$runs" | grep -qv 'success'`, which is
          # the one shape that cannot express "not all successes": grep -qv
          # selects LINES without the word, and $runs is a single line, so
          # ["success","failure"] contains "success", grep matches nothing, the
          # branch is skipped, and the gate falls through to "CI is green".
          # Only an all-failure array took the branch. The gate exists because
          # publishing an untested commit is how a broken build reaches users;
          # a gate that passes a mixed result never provided that.
          if ! printf '%s' "$runs" | python3 -c 'import json,sys; c=json.load(sys.stdin); sys.exit(0 if c and all(x=="success" for x in c) else 1)'; then
            echo "::error::CI is not green on $sha: $runs"
            exit 1
          fi
          echo "CI is green on $sha"

  publish:
    needs: gate
    runs-on: ubuntu-latest
    # A protected environment adds the human step the audit asked for: configure
    # `npm-publish` in repo settings with a required reviewer. Until it is
    # configured GitHub treats it as unprotected, so this line is safe to land
    # before the setting exists and starts enforcing the moment it does.
    environment: npm-publish
    permissions:
      id-token: write # required for OIDC trusted publishing + provenance
      contents: read
    steps:
      # Same default checkout as the gate above, so the commit that was proved
      # green is the commit that gets published.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      # OIDC trusted publishing and provenance need npm >= 11.5.1.
      - name: Upgrade npm
        run: npm install -g npm@latest && npm --version

      - name: Assemble platform packages
        run: node npm/scripts/build.mjs

      - name: Publish
        env:
          NPM_PROVENANCE: "1"
        run: node npm/scripts/publish.mjs ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && '--dry-run' || '' }}
